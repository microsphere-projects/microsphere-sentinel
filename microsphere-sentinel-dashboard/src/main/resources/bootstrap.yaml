# Spring

logging:
  level:
    org:
      springframework:
        web: INFO
  file:
    name: ${user.home}/logs/sentinel/sentinel-dashboard.log
  pattern:
    file: %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
